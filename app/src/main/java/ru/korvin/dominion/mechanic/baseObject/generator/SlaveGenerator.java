package ru.korvin.dominion.mechanic.baseObject.generator;

public class SlaveGenerator {

    /*
    InputStream is = this.getResources().openRawResource(R.raw.creatures);
    Reader reader = new BufferedReader(new InputStreamReader(is));
    Persister serializer = new Persister();
    try
    {
        CreaterList createrList = serializer.read(CreaterList.class, reader, false);
        Log.v("SimpleTest", "Pets in nursery: " + createrList.pars.size());
    }
    catch (Exception e)
    {
        Log.e("SimpleTest", e.getMessage());
    }*/

}
