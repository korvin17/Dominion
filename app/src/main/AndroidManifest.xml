<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.korvin.dominion">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.GirlInfoListActivity"
            android:label="@string/title_girlinfo_list"></activity>
        <activity
            android:name=".activity.GirlInfoDetailActivity"
            android:label="@string/title_girlinfo_detail"
            android:parentActivityName=".activity.GirlInfoListActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="ru.korvin.dominion.GirlInfoListActivity" />
        </activity>
    </application>

</manifest>
